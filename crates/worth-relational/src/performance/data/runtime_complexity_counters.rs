#[derive(Debug, Clone, Default, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct RuntimeComplexityCounters {
    pub full_state_clones: usize,
    pub reconstructive_state_clones: usize,
    pub reconstructive_partitions_materialized: usize,
    pub partition_editions_acquired: usize,
    pub adjacency_kind_slices_leased: usize,
    pub adjacency_relation_ids_copied: usize,
    pub ordinary_partitions_copied_on_write: usize,
    pub ordinary_partition_slots_copied_on_write: usize,
    pub partitions_cloned: usize,
    pub entity_slots_cloned: usize,
    pub relation_slots_cloned: usize,
    pub aosoa_entity_chunks_staged: usize,
    pub aosoa_entity_chunk_slots_materialized: usize,
    pub aosoa_entity_chunks_published: usize,
    pub aosoa_entity_slot_soa_merges: usize,
    pub aosoa_prepare_soa_merge_count: usize,
    pub aosoa_publish_soa_merge_count: usize,
    pub commit_topology_flags: u32,
    pub partitions_touched_by_commit: usize,
    pub entity_slots_touched_by_commit: usize,
    pub relation_slots_touched_by_commit: usize,
    pub bulk_entity_slots_reserved: usize,
    pub bulk_relation_slots_reserved: usize,
    pub relation_identity_candidates_scanned: usize,
    pub visibility_entity_slot_scans: usize,
    pub visibility_relation_slot_scans: usize,
    pub visible_authoritative_entity_records_materialized: usize,
    pub visible_authoritative_relation_records_materialized: usize,
    pub query_packet_count: usize,
    pub query_packet_item_count: usize,
    pub query_packet_peak_width_total: usize,
    pub query_scope_unit_count: usize,
    pub query_parallel_legal_count: usize,
    pub query_parallel_profitable_count: usize,
    pub query_serial_strategy_count: usize,
    pub query_staged_parallel_strategy_count: usize,
    pub query_fragment_scratch_reuse_count: usize,
    pub query_index_attempt_count: usize,
    pub query_index_path_count: usize,
    pub query_index_rejection_count: usize,
    pub query_index_parity_verification_count: usize,
    pub query_index_scratch_reuse_count: usize,
    pub query_authoritative_entity_records_emitted: usize,
    pub query_authoritative_relation_records_emitted: usize,
    pub bulk_mutation_batch_count: usize,
    pub bulk_mutation_entity_target_count: usize,
    pub bulk_mutation_relation_target_count: usize,
    pub bulk_mutation_cross_partition_relation_count: usize,
    pub bulk_mutation_naming_normalization_count: usize,
    pub bulk_mutation_lineage_transition_count: usize,
    pub bulk_mutation_provenance_record_count: usize,
    pub visibility_cache_hits: usize,
    pub visibility_exact_state_materializations: usize,
    pub visibility_cache_miss_reconstructions: usize,
    pub visibility_cache_recent_evictions: usize,
    pub visibility_cache_branch_head_promotions: usize,
    pub visibility_cache_replay_promotions: usize,
    pub visibility_cache_snapshot_promotions: usize,
    pub invariant_entity_slot_scans: usize,
    pub invariant_relation_slot_scans: usize,
    pub invariant_authoritative_entity_records_materialized: usize,
    pub invariant_authoritative_relation_records_materialized: usize,
    pub custom_invariant_preparation_count: usize,
    pub custom_invariant_execution_count: usize,
    pub custom_invariant_panic_count: usize,
    pub custom_invariant_traversal_frontier_count: usize,
    pub custom_invariant_traversal_step_count: usize,
    pub relation_integrity_contracts_evaluated: usize,
    pub relation_endpoint_kind_checks: usize,
    pub relation_cardinality_checks: usize,
    pub relation_cardinality_minimum_certification_contracts_evaluated: usize,
    pub relation_cardinality_minimum_certification_entity_slot_scans: usize,
    pub relation_cardinality_minimum_certification_relation_slot_scans: usize,
    pub relation_uniqueness_checks: usize,
    pub relation_uniqueness_candidates_scanned: usize,
    pub relation_symmetry_checks: usize,
    pub relation_endpoint_deletion_checks: usize,
    pub preparation_packet_count: usize,
    pub preparation_packet_item_count: usize,
    pub preparation_packet_peak_width_total: usize,
    pub preparation_scope_unit_count: usize,
    pub preparation_parallel_legal_count: usize,
    pub preparation_parallel_profitable_count: usize,
    pub preparation_serial_strategy_count: usize,
    pub preparation_staged_parallel_strategy_count: usize,
    pub preparation_reducer_conflict_count: usize,
    pub post_commit_consumer_packet_count: usize,
    pub post_commit_consumer_item_count: usize,
    pub post_commit_consumer_peak_width_total: usize,
    pub post_commit_scope_unit_count: usize,
    pub post_commit_serial_strategy_count: usize,
    pub post_commit_parallel_strategy_count: usize,
    pub snapshot_pin_adjustments: usize,
    pub snapshot_pin_full_rebuilds: usize,
    pub retention_entity_slots_scanned: usize,
    pub retention_relation_slots_scanned: usize,
    pub inspection_structural_identity_lookups: usize,
    pub inspection_structural_identity_query_scans: usize,
    pub inspection_graph_summary_requests: usize,
    pub inspection_kind_summary_requests: usize,
    pub inspection_connectivity_summary_requests: usize,
    pub inspection_connectivity_entity_scans: usize,
    pub inspection_connectivity_relation_scans: usize,
    pub inspection_connectivity_frontier_expansions: usize,
    pub inspection_connectivity_components_evaluated: usize,
    pub inspection_budget_refusals: usize,
    pub inspection_neighbor_requests: usize,
    pub inspection_historical_view_opens: usize,
    pub inspection_commit_reads: usize,
    pub inspection_retention_entity_slot_scans: usize,
    pub inspection_retention_relation_slot_scans: usize,
    pub lineage_finalization_event_batch_width: usize,
    pub lineage_finalization_decision_log_width: usize,
    pub lineage_historical_resolution_requests: usize,
    pub lineage_historical_resolution_event_visits: usize,
    pub lineage_historical_resolution_traversed_events: usize,
    pub lineage_historical_resolution_index_probes: usize,
    pub lineage_historical_resolution_reachable_commit_node_visits: usize,
    pub lineage_historical_resolution_reachable_commit_parent_edge_visits: usize,
    pub lineage_historical_resolution_reachable_commit_catalog_probes: usize,
    pub lineage_branch_divergence_requests: usize,
    pub lineage_branch_divergence_event_scans: usize,
    pub lineage_graph_snapshot_requests: usize,
    pub lineage_graph_snapshot_nodes_materialized: usize,
    pub lineage_graph_snapshot_events_materialized: usize,
    pub lineage_branch_divergence_node_scans: usize,
    pub lineage_graph_snapshot_visibility_cache_hits: usize,
    pub lineage_graph_snapshot_visibility_cache_miss_reconstructions: usize,
    pub lineage_publication_event_width: usize,
    pub lineage_publication_decision_width: usize,
    pub bridge_observation_commit_selections: usize,
    pub bridge_observation_commit_ancestry_visits: usize,
    pub live_entity_history_entries_trimmed: usize,
    pub live_relation_history_entries_trimmed: usize,
    pub forward_adjacency_updates: usize,
    pub reverse_adjacency_updates: usize,
    pub schema_transition_atoms_inspected: usize,
    pub schema_changed_subtrees_inspected: usize,
    pub schema_unchanged_subtrees_reused_by_fingerprint: usize,
    pub schema_bridge_descriptors_built: usize,
    pub schema_normalized_descriptor_compositions: usize,
    pub schema_transition_continue_unchanged_count: usize,
    pub schema_transition_continue_transparent_bridge_count: usize,
    pub schema_transition_continue_visible_bridge_count: usize,
    pub schema_transition_continue_contract_upgrade_count: usize,
    pub schema_transition_require_renegotiation_count: usize,
    pub schema_transition_rejected_count: usize,
    pub schema_historical_interpretation_sensitive_boundaries: usize,
    pub schema_reconciliation_reject_lossy_narrowing_count: usize,
    pub schema_reconciliation_preserve_information_count: usize,
    pub schema_reconciliation_preserve_target_contract_count: usize,
    pub schema_reconciliation_preserve_source_contract_count: usize,
    pub schema_reconciliation_permit_lossy_narrowing_with_annotation_count: usize,
    pub schema_reconciliation_require_explicit_projection_count: usize,
    pub subscriber_resume_evaluations: usize,
    pub subscriber_continue_unchanged_count: usize,
    pub subscriber_continue_transparent_bridge_count: usize,
    pub subscriber_continue_visible_bridge_count: usize,
    pub subscriber_continue_contract_upgrade_count: usize,
    pub subscriber_require_renegotiation_count: usize,
    pub subscriber_rejected_count: usize,
    pub replay_digest_parity_checks: usize,
    pub replay_summary_parity_checks: usize,
    pub replay_deep_artifact_parity_checks: usize,
    pub replay_lineage_authority_lookup_requests: usize,
    pub replay_lineage_log_index_hits: usize,
    pub replay_lineage_checkpoint_index_hits: usize,
    pub replay_lineage_durable_basis_selections: usize,
    pub replay_lineage_retained_envelope_basis_selections: usize,
    pub replay_lineage_authoritative_basis_rejections: usize,
    pub replay_lineage_digest_event_width: usize,
    pub replay_lineage_digest_decision_width: usize,
    pub merge_history_ancestry_traversals: usize,
    pub merge_history_ancestry_nodes_visited: usize,
    pub merge_history_parent_comparisons: usize,
    pub merge_history_replay_parent_checks: usize,
    pub merge_history_replay_planning_nodes_visited: usize,
    pub merge_history_durability_parent_checks: usize,
    pub merge_history_durability_validation_nodes_visited: usize,
    pub merge_planning_requests: usize,
    pub merge_planning_schema_kinds_snapshotted: usize,
    pub merge_planning_target_commits_scoped: usize,
    pub merge_planning_source_commits_scoped: usize,
    pub merge_planning_target_records_scoped: usize,
    pub merge_planning_source_records_scoped: usize,
    pub merge_identity_candidates_discovered: usize,
    pub merge_identity_effective_declarations: usize,
    pub merge_identity_target_records_scanned: usize,
    pub merge_identity_target_records_indexed: usize,
    pub merge_conflict_records_classified: usize,
    pub merge_topology_relation_candidates_scoped: usize,
    pub merge_topology_endpoint_incidences_scoped: usize,
    pub merge_topology_region_conflicts_detected: usize,
    pub merge_topology_region_records_escalated: usize,
    pub merge_causal_records_annotated: usize,
    pub merge_policy_records_resolved: usize,
    pub merge_policy_value_source_state_hits: usize,
    pub merge_policy_value_target_state_hits: usize,
    pub merge_policy_value_base_state_hits: usize,
    pub merge_policy_value_base_patch_authority_hits: usize,
    pub merge_policy_value_missing_ancestor_basis: usize,
    pub merge_policy_value_missing_visible_state: usize,
    pub merge_policy_value_invalid_shape: usize,
    pub merge_lowered_records_emitted: usize,
    pub merge_decision_log_width: usize,
    pub merge_planning_elapsed_nanos: u128,
    pub merge_execution_verification_requests: usize,
    pub merge_execution_branch_head_checks: usize,
    pub merge_execution_merge_base_checks: usize,
    pub merge_execution_schema_kinds_snapshotted: usize,
    pub merge_execution_compiled_plan_digest_checks: usize,
    pub merge_execution_attempts: usize,
    pub merge_execution_requests: usize,
    pub merge_execution_records_admitted: usize,
    pub merge_execution_mutation_intents_emitted: usize,
    pub descriptor_version_mismatches_encountered: usize,
    #[serde(default)]
    pub custom_invariant_candidate_entity_reads: usize,
    #[serde(default)]
    pub custom_invariant_candidate_relation_reads: usize,
    #[serde(default)]
    pub custom_invariant_candidate_adjacency_gathers: usize,
    #[serde(default)]
    pub custom_invariant_candidate_adjacency_count_reads: usize,
    #[serde(default)]
    pub custom_invariant_candidate_adjacency_relation_ids: usize,
    #[serde(default)]
    pub custom_invariant_candidate_reuse_hits: usize,
}
