use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq, Serialize, Deserialize)]
pub struct TransactionTelemetry {
    pub transaction_begin_count: u64,
    pub transaction_commit_count: u64,
    pub transaction_rollback_count: u64,
    pub transaction_poison_count: u64,
    pub rollback_packet_breadth: u64,
    pub rollback_packet_config_count: u64,
    pub rollback_packet_diagnostics_count: u64,
    pub rollback_packet_graph_patch_count: u64,
    pub rollback_packet_created_node_count: u64,
    pub rollback_packet_subscriber_repair_count: u64,
    pub rollback_packet_resource_count: u64,
    pub rollback_packet_temporal_count: u64,
    pub move_transfer_count: u64,
    pub explicit_fork_count: u64,
    pub explicit_snapshot_fork_count: u64,
    pub explicit_fork_denial_count: u64,
    pub restore_transfer_count: u64,
    pub heavy_capture_count: u64,
    pub branch_basis_production_count: u64,
    pub branch_basis_validation_count: u64,
    pub branch_basis_denial_count: u64,
    pub branch_basis_stale_count: u64,
    pub branch_retirement_plan_count: u64,
    pub branch_retirement_execution_count: u64,
    pub branch_retirement_denial_count: u64,
    pub branch_retirement_reclaimed_branch_state_count: u64,
    pub branch_retirement_reclaimed_snapshot_state_count: u64,
    pub branch_retirement_reclaimed_runtime_meta_count: u64,
    pub branch_retirement_retained_proof_count: u64,
    pub branch_targeted_transaction_plan_count: u64,
    pub branch_targeted_transaction_execution_count: u64,
    pub branch_targeted_transaction_denial_count: u64,
    pub branch_targeted_transaction_stale_count: u64,
    pub branch_targeted_transaction_active_switch_avoided_count: u64,
    pub branch_targeted_transaction_touched_node_count: u64,
    pub branch_local_suppressed_observation_count: u64,
    pub foundational_scope_lowering_count: u64,
    pub foundational_scope_lowering_denial_count: u64,
    pub foundational_full_branch_lowering_count: u64,
    pub foundational_selected_node_lowering_count: u64,
    pub foundational_selected_aspect_lowering_count: u64,
    pub scoped_candidate_lowering_count: u64,
    pub scoped_candidate_full_branch_count: u64,
    pub scoped_candidate_selected_node_count: u64,
    pub scoped_candidate_selected_aspect_count: u64,
    pub scoped_candidate_requested_scope_breadth: u64,
    pub scoped_candidate_admitted_breadth: u64,
    pub scoped_candidate_skipped_breadth: u64,
    pub scoped_candidate_no_op_breadth: u64,
    pub scoped_candidate_support_closure_breadth: u64,
    pub scoped_candidate_broad_scan_denial_count: u64,
    pub scoped_merge_admission_count: u64,
    pub scoped_merge_denial_count: u64,
    pub scoped_merge_unavailable_count: u64,
    pub merge_compatibility_build_count: u64,
    pub merge_compatibility_denial_count: u64,
    pub merge_compatibility_readmission_count: u64,
    pub merge_compatibility_readmission_denial_count: u64,
    pub decision_log_event_count: u64,
    pub staged_node_patch_count: u64,
    pub max_touched_nodes_in_txn: u64,
    pub transaction_mark_dirty_candidate_visits: u64,
    pub staged_observation_candidate_count: u64,
    pub staged_observation_match_count: u64,
    pub classified_observation_count: u64,
    pub observation_classification_breadth: u64,
    pub delivered_observation_count: u64,
    pub rollback_suppressed_observation_count: u64,
    /// Nodes visited while walking subscribers forward from a transaction's
    /// staged candidates to find the observed nodes it impacted
    /// (`SignalTransaction::evaluate_observed_demand`). Bounded by the
    /// downstream reach of the change.
    pub observed_demand_reach_visits: u64,
    /// Observed nodes an `evaluate_observed_demand` pass selected as demand
    /// targets because a staged candidate reaches them.
    pub observed_demand_targets: u64,
    /// Settlement passes `evaluate_observed_demand` ran (one pass evaluates
    /// every non-clean node upstream of each target in dependency order).
    pub observed_demand_passes: u64,
}
